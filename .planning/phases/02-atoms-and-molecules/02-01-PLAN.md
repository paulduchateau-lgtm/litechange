---
phase: 02-atoms-and-molecules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/components/atoms/button.html
  - design-system/components/atoms/badge.html
  - design-system/components/atoms/input.html
  - design-system/components/atoms/select.html
autonomous: true
requirements: [ATOM-01, ATOM-02, ATOM-03, ATOM-04]

must_haves:
  truths:
    - "Button renders in primary, secondary, ghost, danger variants at sm/md/lg sizes"
    - "Badge renders in success, warning, error, info, accent variants with colored dot"
    - "Input renders with label, placeholder, focus ring, error state, disabled state"
    - "Select dropdown renders with custom styling matching input component"
    - "All four components work in both light and dark themes via token references only"
  artifacts:
    - path: "design-system/components/atoms/button.html"
      provides: "Button component showcase"
      contains: "liteops-Button--primary"
    - path: "design-system/components/atoms/badge.html"
      provides: "Badge component showcase"
      contains: "liteops-Badge--success"
    - path: "design-system/components/atoms/input.html"
      provides: "Input component showcase"
      contains: "liteops-Input"
    - path: "design-system/components/atoms/select.html"
      provides: "Select component showcase"
      contains: "liteops-Select"
  key_links:
    - from: "all component HTML files"
      to: "design-system/tokens/liteops-tokens.css"
      via: "CSS custom property references"
      pattern: "var\\(--liteops-"
---

<objective>
Build the four most-reused atom components: Button, Badge, Input, and Select. These are the foundational interactive elements that every molecule will compose.

Purpose: Unblock all molecule plans that depend on button, badge, and input patterns.
Output: 4 standalone HTML showcase pages in design-system/components/atoms/
</objective>

<execution_context>
@/Users/paulduchateau/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulduchateau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-atoms-and-molecules/02-CONTEXT.md

<interfaces>
<!-- Token system from Phase 1. All components consume these. -->

Key semantic tokens for interactive components:
```css
--liteops-interactive-primary         /* button primary bg */
--liteops-interactive-primary-hover   /* button primary hover bg */
--liteops-interactive-primary-text    /* button primary text */
--liteops-interactive-secondary       /* transparent */
--liteops-interactive-secondary-hover /* button secondary hover bg */
--liteops-interactive-secondary-text  /* button secondary text */
--liteops-interactive-ghost-hover     /* ghost button hover bg */
--liteops-surface-primary             /* page background */
--liteops-surface-secondary           /* card/panel background */
--liteops-text-primary                /* main text */
--liteops-text-secondary              /* supporting text */
--liteops-text-tertiary               /* labels, hints */
--liteops-text-disabled               /* disabled text */
--liteops-border-primary              /* default border */
--liteops-border-focus                /* focus ring */
--liteops-border-error                /* error border */
--liteops-status-success / -bg        /* badge success */
--liteops-status-warning / -bg        /* badge warning */
--liteops-status-error / -bg          /* badge error */
--liteops-status-info / -bg           /* badge info */
--liteops-accent-primary              /* accent variant */
--liteops-accent-subtle               /* accent bg */
--liteops-accent-text                 /* accent text */
--liteops-radius-sm: 6px
--liteops-radius-md: 10px
--liteops-radius-pill: 9999px
--liteops-duration-normal: 200ms
--liteops-font-body: 'DM Sans'
--liteops-font-mono: 'IBM Plex Mono'
```

Page structure pattern (from token-test.html):
- Standalone HTML with `<!DOCTYPE html>`, `data-theme="light"` on `<html>`
- Google Fonts link for DM Sans, Source Serif 4, IBM Plex Mono
- `<link rel="stylesheet" href="../tokens/liteops-tokens.css">` (note: from components/atoms/ the relative path is `../../tokens/liteops-tokens.css`)
- Inline `<style>` for all component CSS
- Theme toggle button fixed top-right
- Minimal JS (< 15 lines) for theme toggle only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Button and Badge atoms</name>
  <files>design-system/components/atoms/button.html, design-system/components/atoms/badge.html</files>
  <read_first>design-system/tokens/liteops-tokens.css, design-system/pages/token-test.html, CLAUDE.md</read_first>
  <action>
Create two standalone HTML showcase pages following the token-test.html pattern (standalone HTML, inline CSS, Google Fonts link, theme toggle).

**button.html** — Full button component showcase:
- CSS class naming: `.liteops-Button` (base), `--primary`, `--secondary`, `--ghost`, `--danger` (variants), `--sm`, `--md`, `--lg` (sizes)
- Base styles: `font-family: var(--liteops-font-body)`, `font-weight: 500`, `border-radius: var(--liteops-radius-md)`, `cursor: pointer`, `transition: all var(--liteops-duration-normal) ease`, `border: 1px solid transparent`
- Primary: `background: var(--liteops-interactive-primary)`, `color: var(--liteops-interactive-primary-text)`, hover: `background: var(--liteops-interactive-primary-hover)`
- Secondary: `background: var(--liteops-interactive-secondary)`, `border-color: var(--liteops-border-primary)`, `color: var(--liteops-interactive-secondary-text)`, hover: `background: var(--liteops-interactive-secondary-hover)`
- Ghost: `background: transparent`, `color: var(--liteops-text-primary)`, hover: `background: var(--liteops-interactive-ghost-hover)`
- Danger: `background: var(--liteops-status-error)`, `color: var(--liteops-white)`, hover: `opacity: 0.9`
- Sizes: sm `padding: 6px 16px; font-size: 13px`, md `padding: 10px 24px; font-size: 15px`, lg `padding: 14px 32px; font-size: 17px`
- States: `:hover` (per variant above), `:focus-visible` (`outline: 2px solid var(--liteops-border-focus); outline-offset: 2px`), `[disabled]` or `.is-disabled` (`opacity: 0.5; cursor: not-allowed; pointer-events: none`)
- Showcase sections: grid showing all 4 variants x 3 sizes, a disabled row, a focus-visible demonstration row
- Token reference: path from components/atoms/ is `../../tokens/liteops-tokens.css`

**badge.html** — Full badge component showcase:
- CSS class naming: `.liteops-Badge` (base), `--success`, `--warning`, `--error`, `--info`, `--accent` (variants)
- Base styles: `display: inline-flex; align-items: center; gap: 6px; padding: 4px 12px; border-radius: var(--liteops-radius-pill); font-family: var(--liteops-font-mono); font-size: 10px; font-weight: 500; text-transform: uppercase; letter-spacing: 0.1em`
- Each variant: colored dot (8px circle via `::before` pseudo-element) + tinted background + matching text
- Success: dot `var(--liteops-status-success)`, bg `var(--liteops-status-success-bg)`, text `var(--liteops-status-success)`
- Warning: dot/text `var(--liteops-status-warning)`, bg `var(--liteops-status-warning-bg)`
- Error: dot/text `var(--liteops-status-error)`, bg `var(--liteops-status-error-bg)`
- Info: dot/text `var(--liteops-status-info)`, bg `var(--liteops-status-info-bg)`
- Accent: dot/text `var(--liteops-accent-text)`, bg `var(--liteops-accent-subtle)`
- Showcase sections: all 5 variants shown, dark theme preview via inline `data-theme="dark"` panel

No hardcoded color values anywhere. Every color via `var(--liteops-*)`.
  </action>
  <verify>
    <automated>grep -c "var(--liteops-" design-system/components/atoms/button.html && grep -c "var(--liteops-" design-system/components/atoms/badge.html && grep -L "liteops-Button--primary" design-system/components/atoms/button.html | wc -l | grep "0" && grep -L "liteops-Badge--success" design-system/components/atoms/badge.html | wc -l | grep "0"</automated>
  </verify>
  <acceptance_criteria>
- grep "liteops-Button--primary" button.html returns matches
- grep "liteops-Button--secondary" button.html returns matches
- grep "liteops-Button--ghost" button.html returns matches
- grep "liteops-Button--danger" button.html returns matches
- grep "liteops-Button--sm" button.html returns matches
- grep "liteops-Button--lg" button.html returns matches
- grep "is-disabled" button.html returns matches
- grep "focus-visible" button.html returns matches
- grep "liteops-Badge--success" badge.html returns matches
- grep "liteops-Badge--warning" badge.html returns matches
- grep "liteops-Badge--error" badge.html returns matches
- grep "liteops-Badge--info" badge.html returns matches
- grep "liteops-Badge--accent" badge.html returns matches
- grep -c "#[0-9A-Fa-f]\{6\}" button.html returns 0 or only in theme toggle section
- grep "data-theme" button.html returns matches (theme toggle present)
  </acceptance_criteria>
  <done>Button renders in 4 variants x 3 sizes with hover/focus/disabled states. Badge renders in 5 variants with colored dot. Both work in light and dark themes. Zero hardcoded colors in component CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Build Input and Select atoms</name>
  <files>design-system/components/atoms/input.html, design-system/components/atoms/select.html</files>
  <read_first>design-system/tokens/liteops-tokens.css, design-system/components/atoms/button.html, CLAUDE.md</read_first>
  <action>
Create two standalone HTML showcase pages following the same page pattern established in Task 1.

**input.html** — Full text input showcase:
- CSS class naming: `.liteops-Input` (base), `.liteops-Input__label`, `.liteops-Input__field`, `.liteops-Input__helper`, `.liteops-Input__error`
- Field base: `width: 100%; padding: 10px 14px; font-family: var(--liteops-font-body); font-size: 15px; background: var(--liteops-surface-secondary); border: 1px solid var(--liteops-border-primary); border-radius: var(--liteops-radius-sm); color: var(--liteops-text-primary); transition: border-color var(--liteops-duration-normal) ease`
- Label: `font-family: var(--liteops-font-body); font-size: 14px; font-weight: 500; color: var(--liteops-text-primary); margin-bottom: 6px; display: block`
- Placeholder: `color: var(--liteops-text-tertiary)`
- Focus: `border-color: var(--liteops-border-focus); outline: 2px solid var(--liteops-border-focus); outline-offset: 2px`
- Error state: `.is-error` on wrapper, field gets `border-color: var(--liteops-border-error)`, show `.liteops-Input__error` text in `color: var(--liteops-status-error); font-size: 13px; margin-top: 4px`
- Disabled state: `.is-disabled`, field gets `opacity: 0.5; cursor: not-allowed; background: var(--liteops-surface-tertiary)`
- Helper text: `font-size: 13px; color: var(--liteops-text-tertiary); margin-top: 4px`
- Showcase sections: default, with helper text, error state, disabled state, dark theme panel

**select.html** — Full select dropdown showcase:
- CSS class naming: `.liteops-Select` (wrapper), `.liteops-Select__label`, `.liteops-Select__field`, `.liteops-Select__icon` (chevron)
- Field: same base styles as Input field for visual consistency, plus `appearance: none; padding-right: 36px` (space for custom chevron)
- Custom chevron: absolute-positioned SVG chevron-down icon (inline SVG in CSS via `background-image: url("data:image/svg+xml,...")` or positioned `::after` pseudo-element)
- States: default, focus (same ring as input), disabled (same treatment as input), error
- Showcase sections: default, with options populated, disabled, error, dark theme panel

Both files import tokens via `<link rel="stylesheet" href="../../tokens/liteops-tokens.css">`.
No hardcoded colors.
  </action>
  <verify>
    <automated>grep -c "var(--liteops-" design-system/components/atoms/input.html && grep -c "var(--liteops-" design-system/components/atoms/select.html && grep -q "liteops-Input__field" design-system/components/atoms/input.html && grep -q "liteops-Select__field" design-system/components/atoms/select.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- grep "liteops-Input__field" input.html returns matches
- grep "liteops-Input__label" input.html returns matches
- grep "liteops-Input__error" input.html returns matches
- grep "is-error" input.html returns matches
- grep "is-disabled" input.html returns matches
- grep "focus-visible" input.html returns matches OR grep "border-focus" input.html returns matches
- grep "liteops-Select__field" select.html returns matches
- grep "liteops-Select__label" select.html returns matches
- grep "appearance: none" select.html returns matches
- grep "data-theme" input.html returns matches
- grep "data-theme" select.html returns matches
  </acceptance_criteria>
  <done>Input renders with label, placeholder, focus ring, error state, disabled state. Select renders with custom styling matching input look. Both work in both themes.</done>
</task>

</tasks>

<verification>
- Open each HTML file in browser: all 4 components render correctly in light theme
- Click theme toggle: all 4 components switch to dark theme with correct token remapping
- No visual glitches, no hardcoded colors visible in inspector
- Button hover/focus/disabled states all functional via CSS only
- Badge dots visible with correct colors per variant
- Input focus ring appears on tab navigation
- Select chevron visible and properly positioned
</verification>

<success_criteria>
4 standalone HTML pages exist in design-system/components/atoms/, each showcasing all variants and states of their component in both themes, using only liteops- token references.
</success_criteria>

<output>
After completion, create `.planning/phases/02-atoms-and-molecules/02-01-SUMMARY.md`
</output>
