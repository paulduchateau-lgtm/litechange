---
phase: 02-atoms-and-molecules
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - design-system/components/molecules/tab-pill.html
  - design-system/components/molecules/progress-bar.html
  - design-system/components/molecules/toast.html
  - design-system/pages/chart-colors.html
autonomous: true
requirements: [MOLC-06, MOLC-07, MOLC-08, MOLC-09]

must_haves:
  truths:
    - "Tab/pill navigation shows active state and content switching indication"
    - "Progress bar renders with threshold markers and percentage label"
    - "Toast renders with auto-dismiss indication and dismiss button"
    - "Chart color documentation shows 6-color sequence per theme with contrast verification"
  artifacts:
    - path: "design-system/components/molecules/tab-pill.html"
      provides: "Tab/pill molecule showcase"
      contains: "liteops-Tabs"
    - path: "design-system/components/molecules/progress-bar.html"
      provides: "Progress bar molecule showcase"
      contains: "liteops-ProgressBar"
    - path: "design-system/components/molecules/toast.html"
      provides: "Toast molecule showcase"
      contains: "liteops-Toast"
    - path: "design-system/pages/chart-colors.html"
      provides: "Chart color system documentation"
      contains: "chart-primary"
  key_links:
    - from: "chart-colors.html"
      to: "design-system/tokens/liteops-tokens.css"
      via: "chart token references"
      pattern: "liteops-chart-"
---

<objective>
Build the remaining 4 molecule components: Tab/Pill navigation, Progress Bar, Toast notification, and Chart Color System documentation. These complete the molecule layer.

Purpose: Finish the Phase 2 component inventory. Chart colors are a critical reference for Phase 3 data visualization organisms.
Output: 3 molecule pages + 1 documentation page
</objective>

<execution_context>
@/Users/paulduchateau/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulduchateau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-atoms-and-molecules/02-CONTEXT.md
@.planning/phases/02-atoms-and-molecules/02-01-SUMMARY.md

<interfaces>
Key tokens for these molecules:
```css
/* Tabs */
--liteops-accent-primary             /* active tab indicator */
--liteops-accent-text                /* active tab text */
--liteops-accent-subtle              /* pill active bg */
--liteops-text-secondary             /* inactive tab text */
--liteops-border-secondary           /* tab underline border */
--liteops-interactive-ghost-hover    /* tab hover */
--liteops-radius-pill: 9999px        /* pill shape */
--liteops-radius-sm: 6px             /* pill container */

/* Progress */
--liteops-accent-primary             /* filled bar */
--liteops-surface-tertiary           /* empty track */
--liteops-status-success             /* completed state */
--liteops-status-warning             /* threshold warning */
--liteops-status-error               /* below threshold */

/* Toast */
--liteops-surface-elevated           /* toast bg */
--liteops-shadow-lg                  /* toast shadow */
--liteops-status-*                   /* variant colors */

/* Chart */
--liteops-chart-primary              /* 1st color */
--liteops-chart-secondary            /* 2nd color */
--liteops-chart-tertiary             /* 3rd color */
--liteops-chart-quaternary           /* 4th color */
--liteops-chart-grid                 /* grid lines */
--liteops-chart-axis                 /* axis labels */
```

Button atom classes from Plan 01: .liteops-Button--ghost--sm (for toast dismiss)
Badge atom classes from Plan 01: .liteops-Badge--* (for tab badges)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Tab/Pill and Progress Bar molecules</name>
  <files>design-system/components/molecules/tab-pill.html, design-system/components/molecules/progress-bar.html</files>
  <read_first>design-system/tokens/liteops-tokens.css, design-system/components/atoms/button.html, CLAUDE.md</read_first>
  <action>
Create two standalone HTML showcase pages.

**tab-pill.html** — Tab and Pill navigation molecule:
- Two distinct variants on same page: Line Tabs and Pill Tabs

LINE TABS:
- CSS class naming: `.liteops-Tabs` (wrapper), `__list` (ul), `__tab` (button/link), `.is-active`
- List: `display: flex; gap: 0; border-bottom: 1px solid var(--liteops-border-secondary)`
- Tab: `padding: var(--liteops-space-3) var(--liteops-space-4); font-family: var(--liteops-font-body); font-size: 14px; font-weight: 500; color: var(--liteops-text-secondary); background: transparent; border: none; cursor: pointer; position: relative; transition: color var(--liteops-duration-normal) ease`
- Tab hover: `color: var(--liteops-text-primary)`
- Tab active: `color: var(--liteops-accent-text)`, plus a `::after` pseudo-element bottom border: `content: ''; position: absolute; bottom: -1px; left: 0; right: 0; height: 2px; background: var(--liteops-accent-primary); border-radius: 1px`
- Focus: `outline: 2px solid var(--liteops-border-focus); outline-offset: -2px`

PILL TABS:
- CSS class naming: `.liteops-PillTabs` (wrapper), `__list`, `__pill`, `.is-active`
- List: `display: flex; gap: var(--liteops-space-1); padding: var(--liteops-space-1); background: var(--liteops-surface-tertiary); border-radius: var(--liteops-radius-sm)`
- Pill: `padding: 6px 16px; font-family: var(--liteops-font-body); font-size: 13px; font-weight: 500; color: var(--liteops-text-secondary); background: transparent; border: none; border-radius: var(--liteops-radius-sm); cursor: pointer; transition: all var(--liteops-duration-normal) ease`
- Pill active: `background: var(--liteops-surface-elevated); color: var(--liteops-text-primary); box-shadow: var(--liteops-shadow-xs)`
- Showcase: line tabs with 3 items, pill tabs with 3 items, content switching indication (use CSS :target or show/hide pattern), dark theme panel

**progress-bar.html** — Progress bar molecule:
- CSS class naming: `.liteops-ProgressBar` (wrapper), `__track`, `__fill`, `__label`, `__value`, `__threshold`
- Track: `width: 100%; height: 8px; background: var(--liteops-surface-tertiary); border-radius: 4px; overflow: hidden; position: relative`
- Fill: `height: 100%; border-radius: 4px; transition: width var(--liteops-duration-slow) var(--liteops-easing-out)`. Color varies by level: default `var(--liteops-accent-primary)`, success (>80%) `var(--liteops-status-success)`, warning (40-60%) `var(--liteops-status-warning)`, error (<30%) `var(--liteops-status-error)`
- Label: `font-family: var(--liteops-font-body); font-size: 13px; color: var(--liteops-text-secondary); margin-bottom: var(--liteops-space-1)`
- Value: `font-family: var(--liteops-font-mono); font-size: 11px; font-weight: 500; color: var(--liteops-text-primary)` — show "78%" at right end
- Threshold marker: vertical line at threshold position (e.g., 70% mark). `position: absolute; top: 0; bottom: 0; width: 2px; background: var(--liteops-text-tertiary)` with a small label below it
- Showcase: 10%, 45%, 78%, 100% fill examples, with threshold markers at 70%, error/warning/success color variants, with label and percentage, dark theme panel
  </action>
  <verify>
    <automated>grep -q "liteops-Tabs__tab" design-system/components/molecules/tab-pill.html && grep -q "liteops-PillTabs" design-system/components/molecules/tab-pill.html && grep -q "is-active" design-system/components/molecules/tab-pill.html && grep -q "liteops-ProgressBar__fill" design-system/components/molecules/progress-bar.html && grep -q "liteops-ProgressBar__threshold" design-system/components/molecules/progress-bar.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- grep "liteops-Tabs__tab" tab-pill.html returns matches
- grep "liteops-PillTabs__pill" tab-pill.html returns matches
- grep "is-active" tab-pill.html returns matches
- grep "border-bottom" tab-pill.html returns matches (line tab underline)
- grep "focus-visible" tab-pill.html returns matches OR grep "border-focus" tab-pill.html returns matches
- grep "liteops-ProgressBar__track" progress-bar.html returns matches
- grep "liteops-ProgressBar__fill" progress-bar.html returns matches
- grep "liteops-ProgressBar__threshold" progress-bar.html returns matches
- grep "font-mono" progress-bar.html returns matches (percentage label)
- grep "data-theme" tab-pill.html returns matches
- grep "data-theme" progress-bar.html returns matches
  </acceptance_criteria>
  <done>Line tabs and pill tabs render with active indicators. Progress bar renders with fill, threshold markers, and percentage label. Both work in both themes.</done>
</task>

<task type="auto">
  <name>Task 2: Build Toast molecule and Chart Colors documentation</name>
  <files>design-system/components/molecules/toast.html, design-system/pages/chart-colors.html</files>
  <read_first>design-system/tokens/liteops-tokens.css, design-system/components/molecules/alert.html, CLAUDE.md</read_first>
  <action>
Create two standalone HTML pages.

**toast.html** — Toast notification molecule:
- CSS class naming: `.liteops-Toast` (base), `--success`, `--warning`, `--error`, `--info`, `__icon`, `__content`, `__title`, `__description`, `__dismiss`, `__timer`
- Base: `display: flex; align-items: flex-start; gap: var(--liteops-space-3); padding: var(--liteops-space-4) var(--liteops-space-5); background: var(--liteops-surface-elevated); border-radius: var(--liteops-radius-md); box-shadow: var(--liteops-shadow-lg); border: 1px solid var(--liteops-border-secondary); min-width: 320px; max-width: 420px; position: relative`
- Variants: colored left accent bar (like alert) + icon area. Success/warning/error/info use same status tokens as Alert.
- Title: `font-family: var(--liteops-font-body); font-size: 14px; font-weight: 600; color: var(--liteops-text-primary)`
- Description: `font-size: 13px; color: var(--liteops-text-secondary); margin-top: 2px`
- Dismiss button: `position: absolute; top: var(--liteops-space-3); right: var(--liteops-space-3)`, small X icon, `color: var(--liteops-text-tertiary)`, hover: `color: var(--liteops-text-primary)`
- Auto-dismiss indication: `.liteops-Toast__timer` — a thin progress bar at the bottom of the toast that visually indicates time remaining. `height: 2px; background: var(--liteops-accent-primary); animation: liteops-toast-timer 5s linear forwards`. Define `@keyframes liteops-toast-timer { from { width: 100%; } to { width: 0%; } }`
- Stacking: show 3 toasts stacked vertically with 8px gap to demonstrate the toast stack pattern
- Showcase: all 4 variants, with timer animation, with dismiss button, stacked example, dark theme panel
- Respect `prefers-reduced-motion`: timer animation none

**chart-colors.html** — Chart color system documentation:
- Page title: "Systeme de couleurs graphiques" (French)
- Follow the token-test.html page pattern (page header, sections, theme toggle). Place in design-system/pages/ since this is documentation.
- Token path: `../tokens/liteops-tokens.css`

- Section 1 — Color Sequence: show the ordered 6-color sequence for EACH theme:
  - Light theme: 1. lite-700 (#6B8A1A), 2. signal-500 (#4A90B8), 3. warm-500 (#C45A32), 4. lite-300 (#C8FF3C), 5. signal-300 (#7AB4D4), 6. warm-300 (#E08A68)
  - Dark theme: 1. lite-400 (#B0D838), 2. signal-300 (#7AB4D4), 3. warm-300 (#E08A68), 4. lite-600 (#84A422), 5. signal-700 (#2D6888), 6. warm-700 (#8A3518)
  - Display as large circles (48px) in a horizontal row, numbered, with hex value and token name below each

- Section 2 — Usage Pattern: show a mock bar chart using CSS-only bars in the chart color sequence. 6 bars, each in the next color. Labels in IBM Plex Mono 10px UPPERCASE. Grid lines in `var(--liteops-chart-grid)`. Axis labels in `var(--liteops-chart-axis)`.

- Section 3 — Contrast Verification: for each chart color, show its contrast ratio against:
  - surface-primary (the chart background)
  - surface-secondary (if used in a card context)
  - Display as a table with: Color | Token | vs surface-primary | vs surface-secondary | Pass/Fail
  - Use WCAG AA standard for graphical objects: 3:1 minimum
  - Show ratios in IBM Plex Mono with green PASS / red FAIL badges

- Section 4 — Grid and Axis: show token references for chart infrastructure colors (grid lines, axis, label color)
  </action>
  <verify>
    <automated>grep -q "liteops-Toast__timer" design-system/components/molecules/toast.html && grep -q "liteops-Toast--success" design-system/components/molecules/toast.html && grep -q "liteops-toast-timer" design-system/components/molecules/toast.html && grep -q "chart-primary" design-system/pages/chart-colors.html && grep -q "Contrast" design-system/pages/chart-colors.html && grep -q "3:1" design-system/pages/chart-colors.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- grep "liteops-Toast" toast.html returns matches
- grep "liteops-Toast--success" toast.html returns matches
- grep "liteops-Toast--warning" toast.html returns matches
- grep "liteops-Toast--error" toast.html returns matches
- grep "liteops-Toast--info" toast.html returns matches
- grep "liteops-Toast__dismiss" toast.html returns matches
- grep "liteops-Toast__timer" toast.html returns matches
- grep "liteops-toast-timer" toast.html returns matches (keyframes)
- grep "prefers-reduced-motion" toast.html returns matches
- grep "chart-primary" chart-colors.html returns matches
- grep "chart-secondary" chart-colors.html returns matches
- grep "lite-700" chart-colors.html returns matches OR grep "6B8A1A" chart-colors.html returns matches
- grep "lite-400" chart-colors.html returns matches OR grep "B0D838" chart-colors.html returns matches
- grep "3:1" chart-colors.html returns matches (WCAG graphical objects threshold)
- grep "data-theme" toast.html returns matches
- grep "data-theme" chart-colors.html returns matches
  </acceptance_criteria>
  <done>Toast renders in 4 variants with dismiss button and auto-dismiss timer animation. Chart colors page documents 6-color sequence per theme, shows CSS-only mock chart, and verifies contrast ratios at 3:1 for graphical objects.</done>
</task>

</tasks>

<verification>
- Open all 4 pages in browser
- Toast: timer animation runs (shrinking bar), dismiss button visible, 4 color variants distinct
- Tab/pill: active states clear, pill tabs have contained background
- Progress bar: fill widths match percentages, threshold markers visible
- Chart colors: 6-color sequence displayed for both themes, mock chart renders, contrast table shows ratios
- All pages toggle between light/dark themes correctly
</verification>

<success_criteria>
3 molecule pages (tab-pill, progress-bar, toast) and 1 documentation page (chart-colors) complete the Phase 2 component inventory. All 21 requirements addressed across 5 plans.
</success_criteria>

<output>
After completion, create `.planning/phases/02-atoms-and-molecules/02-05-SUMMARY.md`
</output>
