---
phase: 03-organisms
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/components/organisms/agent-card.html
  - design-system/components/organisms/pipeline-viz.html
  - design-system/components/organisms/multi-select.html
autonomous: true
requirements: [ORGN-06, ORGN-07, ORGN-10]

must_haves:
  truths:
    - "Agent card renders with agent name, status badge, log preview, and action buttons"
    - "Pipeline visualization shows connected nodes with status colors"
    - "Multi-select dropdown has search filtering and tag display using badges"
    - "All three organisms work in both light and dark themes"
  artifacts:
    - path: "design-system/components/organisms/agent-card.html"
      provides: "Agent card organism"
      contains: "liteops-AgentCard"
    - path: "design-system/components/organisms/pipeline-viz.html"
      provides: "Pipeline visualization organism"
      contains: "liteops-Pipeline"
    - path: "design-system/components/organisms/multi-select.html"
      provides: "Multi-select dropdown organism"
      contains: "liteops-MultiSelect"
  key_links:
    - from: "agent-card.html"
      to: "badge.html atom"
      via: "composes .liteops-Badge for status indicator"
      pattern: "liteops-Badge"
    - from: "agent-card.html"
      to: "button.html atom"
      via: "composes .liteops-Button for action buttons"
      pattern: "liteops-Button"
    - from: "multi-select.html"
      to: "badge.html atom"
      via: "composes .liteops-Badge for selected tags"
      pattern: "liteops-Badge"
---

<objective>
Build the three domain-specific and utility organisms: agent card, pipeline visualization, and multi-select dropdown. These are the most distinctive organisms in the LiteOps system.

Purpose: Agent card and pipeline viz are NAMIBIA/AI-specific components unique to LiteOps. Multi-select is a complex input organism needed for tag assignment, team selection, and dimension filtering across the app.
Output: Three standalone HTML showcase pages in design-system/components/organisms/
</objective>

<execution_context>
@/Users/paulduchateau/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulduchateau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-organisms/03-CONTEXT.md
@design-system/tokens/liteops-tokens.css
@design-system/components/atoms/badge.html
@design-system/components/atoms/button.html
@design-system/components/atoms/input.html

<interfaces>
<!-- Badge atom classes -->
.liteops-Badge — pill shape, IBM Plex Mono 10px uppercase, colored dot
.liteops-Badge--success/--warning/--error/--info/--accent — variants

<!-- Button atom classes -->
.liteops-Button--primary/--secondary/--ghost/--danger — variants
.liteops-Button--sm/--md/--lg — sizes

<!-- Input atom classes (from input.html) -->
.liteops-Input — DM Sans 14px, border, radius-sm, focus ring
.liteops-Input:focus — border-focus color, ring

<!-- Status color tokens -->
--liteops-status-success / --liteops-status-warning / --liteops-status-error / --liteops-status-info
--liteops-status-success-bg / --liteops-status-warning-bg / --liteops-status-error-bg
--liteops-lite-400 (active nodes in dark), --liteops-lite-700 (active in light)
--liteops-paper-500 (pending), --liteops-warm-500 (error)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build agent card organism</name>
  <files>design-system/components/organisms/agent-card.html</files>
  <read_first>
    - design-system/components/atoms/badge.html (Badge for status indicator)
    - design-system/components/atoms/button.html (Button for actions)
    - design-system/tokens/liteops-tokens.css (surface and status tokens)
  </read_first>
  <action>
Create standalone HTML showcase page for the agent card organism at design-system/components/organisms/agent-card.html.

**Agent Card component CSS (.liteops-AgentCard):**
- BEM: .liteops-AgentCard, .liteops-AgentCard__header, .liteops-AgentCard__name, .liteops-AgentCard__status, .liteops-AgentCard__log, .liteops-AgentCard__logEntry, .liteops-AgentCard__actions, .liteops-AgentCard__timestamp
- Background: var(--liteops-surface-secondary)
- Border: 1px solid var(--liteops-border-primary)
- Border-radius: var(--liteops-radius-md)
- Box-shadow: var(--liteops-shadow-sm)
- Overflow: hidden

**Agent Card header (.liteops-AgentCard__header):**
- Display: flex, align-items: center, justify-content: space-between
- Padding: var(--liteops-space-4) var(--liteops-space-5)
- Border-bottom: 1px solid var(--liteops-border-secondary)
- Agent icon: 36px circle with accent-subtle background, robot/AI SVG icon (16px) in accent color
- Name (.liteops-AgentCard__name): DM Sans 15px, font-weight: 600, var(--liteops-text-primary)
- Status: compose .liteops-Badge (copy badge CSS into style block). Variants: "Actif" (success), "En pause" (warning), "Erreur" (error), "Inactif" (info)

**Log preview (.liteops-AgentCard__log):**
- Padding: var(--liteops-space-4) var(--liteops-space-5)
- Max-height: 160px, overflow-y: auto
- Each log entry (.liteops-AgentCard__logEntry): flex column
  - Timestamp: IBM Plex Mono 10px, var(--liteops-text-tertiary), uppercase
  - Message: DM Sans 13px, var(--liteops-text-secondary)
  - Separator: 1px border-secondary between entries, padding: var(--liteops-space-3) 0
- Truncation approach: show last 4-5 log entries, oldest at top, newest at bottom

**Actions (.liteops-AgentCard__actions):**
- Padding: var(--liteops-space-3) var(--liteops-space-5)
- Border-top: 1px solid var(--liteops-border-secondary)
- Background: var(--liteops-surface-tertiary)
- Display: flex, gap: var(--liteops-space-3)
- Compose .liteops-Button--ghost--sm ("Voir logs") and .liteops-Button--primary--sm ("Relancer") or .liteops-Button--danger--sm ("Arreter")

**Sample agents (French, NAMIBIA domain):**
- "Agent OCR" — Actif, logs: "Document traite: facture_2024_03.pdf", "Extraction terminee: 12 champs identifies", "Envoi vers pipeline RAG"
- "Agent RAG" — En pause, logs: "Index mis a jour: 1,247 documents", "Attente de nouveaux documents"
- "Agent Analyse" — Erreur, logs: "Erreur connexion base de donnees", "Tentative de reconnexion (3/5)"

**Showcase sections:**
1. "Active Agent" — single agent card in active state with recent logs
2. "Agent States" — 3 cards side by side showing Active, Paused, Error states (use flex row with gap-6, each card 320px wide)
3. "Dark Theme" — agent cards in data-theme="dark" panel (this is their primary context since agents appear in dashboards)
4. "Compact Variant" — .liteops-AgentCard--compact: no log preview, just header + actions. Useful in grid layouts.

**Accessibility:** Each card has role="article" and aria-label with agent name. Log section has role="log" and aria-label="Journal d'activite". Status badge is announced via aria-label.
  </action>
  <verify>
    <automated>test -f design-system/components/organisms/agent-card.html && grep -q "liteops-AgentCard" design-system/components/organisms/agent-card.html && grep -q "liteops-Badge" design-system/components/organisms/agent-card.html && grep -q "liteops-Button" design-system/components/organisms/agent-card.html && grep -q "Agent OCR\|Agent RAG\|Agent Analyse" design-system/components/organisms/agent-card.html && grep -q "role=\"log\"" design-system/components/organisms/agent-card.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "liteops-AgentCard" agent-card.html finds the component block
    - grep "liteops-AgentCard__header\|liteops-AgentCard__log\|liteops-AgentCard__actions" finds all sections
    - grep "liteops-Badge" agent-card.html confirms status badge composition
    - grep "liteops-Button" agent-card.html confirms action button composition
    - grep "Agent OCR" agent-card.html confirms NAMIBIA domain content
    - grep "role=\"log\"" agent-card.html confirms log section accessibility
    - File opens in browser showing agent cards with status, logs, and actions
  </acceptance_criteria>
  <done>Agent card renders with header (name + status badge), scrollable log preview with timestamps, and action bar with composed buttons. Three agent states shown. Works in both themes with emphasis on dark dashboard context.</done>
</task>

<task type="auto">
  <name>Task 2: Build pipeline visualization and multi-select organisms</name>
  <files>design-system/components/organisms/pipeline-viz.html, design-system/components/organisms/multi-select.html</files>
  <read_first>
    - design-system/components/atoms/badge.html (Badge for pipeline status and multi-select tags)
    - design-system/components/atoms/input.html (Input for multi-select search field)
    - design-system/tokens/liteops-tokens.css (status colors, accent tokens)
    - CLAUDE.md (chart colors per theme, animations: only pulse-glow on observability dots)
  </read_first>
  <action>
Create TWO standalone HTML showcase pages.

**FILE 1: design-system/components/organisms/pipeline-viz.html**

Pipeline Visualization component CSS (.liteops-Pipeline):
- BEM: .liteops-Pipeline, .liteops-Pipeline__track, .liteops-Pipeline__node, .liteops-Pipeline__connector, .liteops-Pipeline__label, .liteops-Pipeline__status
- Layout: VERTICAL flow (top to bottom, per existing docs/data-pipeline.html pattern)
- Track: display flex, flex-direction column, align-items center, gap: 0 (connectors fill the gap visually)

Pipeline node (.liteops-Pipeline__node):
- Width: 280px (or 100% within a constrained parent)
- Background: var(--liteops-surface-elevated)
- Border: 1px solid var(--liteops-border-primary)
- Border-radius: var(--liteops-radius-md)
- Padding: var(--liteops-space-4) var(--liteops-space-5)
- Display: flex, align-items: center, gap: var(--liteops-space-4)

Node status indicator:
- 12px circle on the left side
- Status colors per CONTEXT.md: var(--liteops-accent-primary) for active/complete (lite-400 in dark, lite-700 in light), var(--liteops-paper-500) for pending, var(--liteops-status-error) for error
- Active nodes get pulse-glow animation (the ONLY allowed continuous animation per CLAUDE.md):
  @keyframes pulse-glow { 0%,100% { box-shadow: 0 0 0 0 rgba(176,216,56,0.4); } 50% { box-shadow: 0 0 8px 4px rgba(176,216,56,0.15); } }
  animation: pulse-glow var(--liteops-loop-pulse-glow) infinite

Node label: DM Sans 14px, font-weight: 500, var(--liteops-text-primary)
Node description: DM Sans 12px, var(--liteops-text-tertiary)
Step number: IBM Plex Mono 11px, var(--liteops-accent-text), inside a 24px circle with accent-subtle background

Connector (.liteops-Pipeline__connector):
- Width: 2px, height: 32px, background: var(--liteops-border-primary)
- Centered between nodes
- Active connectors (between two complete nodes): background: var(--liteops-accent-primary)

Sample pipeline (NAMIBIA data pipeline):
1. "Collecte documents" — Complete (green)
2. "Extraction OCR" — Complete (green)
3. "Enrichissement RAG" — Active (green, pulsing)
4. "Analyse semantique" — Pending (grey)
5. "Generation rapport" — Pending (grey)

Showcase sections:
1. "Default Pipeline" — 5-step pipeline with mixed statuses
2. "All Complete" — same pipeline all green
3. "Error State" — pipeline where step 3 has error status, steps 4-5 pending
4. "Dark Theme" — pipeline in data-theme="dark" panel (this is the primary context)
5. "Compact Variant" — .liteops-Pipeline--compact: smaller nodes (no description), narrower width, for sidebar or small panels

**FILE 2: design-system/components/organisms/multi-select.html**

Multi-Select component CSS (.liteops-MultiSelect):
- BEM: .liteops-MultiSelect, .liteops-MultiSelect__trigger, .liteops-MultiSelect__tags, .liteops-MultiSelect__search, .liteops-MultiSelect__dropdown, .liteops-MultiSelect__option, .liteops-MultiSelect__clear
- Width: 100%, max-width: 400px (for showcase)

Trigger area (.liteops-MultiSelect__trigger):
- Border: 1px solid var(--liteops-border-primary), border-radius: var(--liteops-radius-sm)
- Min-height: 40px, padding: var(--liteops-space-2) var(--liteops-space-3)
- Background: var(--liteops-surface-secondary)
- Display: flex, flex-wrap: wrap, gap: var(--liteops-space-2), align-items: center
- Focus state: border-color: var(--liteops-border-focus), outline: 2px solid var(--liteops-border-focus), outline-offset: 2px

Tags (.liteops-MultiSelect__tags): compose .liteops-Badge--accent for selected items. Each tag has a small X button (12px) for removal. Use inline-flex for each tag.

Search input (.liteops-MultiSelect__search): minimal input (no border, flex-grow, DM Sans 14px) inside the trigger area, placeholder "Rechercher..."

Dropdown (.liteops-MultiSelect__dropdown):
- Position: absolute (relative to trigger parent)
- Background: var(--liteops-surface-elevated)
- Border: 1px solid var(--liteops-border-primary)
- Border-radius: var(--liteops-radius-sm)
- Box-shadow: var(--liteops-shadow-lg)
- Max-height: 240px, overflow-y: auto
- Margin-top: var(--liteops-space-1)

Options (.liteops-MultiSelect__option):
- Padding: var(--liteops-space-2) var(--liteops-space-3)
- DM Sans 14px, var(--liteops-text-primary)
- Hover: var(--liteops-accent-subtle) background
- Selected: checkmark icon on right, accent-text color
- .is-highlighted: accent-subtle background (keyboard navigation indicator)

Clear all (.liteops-MultiSelect__clear): ghost button "Tout effacer" at bottom of tag area or right side

**Since showcase is CSS-only:** Show the multi-select in STATIC states (not interactive). Show:
1. "Empty State" — trigger with placeholder text, no tags
2. "With Selections" — trigger showing 3 selected tags (badges) + search input
3. "Open Dropdown" — trigger + dropdown visible below showing 8 options, 3 checked. Use position: relative (not absolute) for showcase visibility
4. "Filtered Dropdown" — trigger with "Res" typed in search, dropdown showing filtered results ("Resistance au changement", "Restructuration")
5. "Dark Theme" — same states in data-theme="dark" panel

Sample options (change management domain): "Resistance au changement", "Communication interne", "Formation", "Restructuration", "Migration SI", "Transformation digitale", "Conduite du changement", "Gestion des risques"

**Accessibility:** trigger has role="combobox", aria-expanded, aria-haspopup="listbox". Dropdown has role="listbox". Options have role="option" and aria-selected. Tags are announced as "X selectionne, appuyez sur Supprimer pour retirer".
  </action>
  <verify>
    <automated>test -f design-system/components/organisms/pipeline-viz.html && test -f design-system/components/organisms/multi-select.html && grep -q "liteops-Pipeline" design-system/components/organisms/pipeline-viz.html && grep -q "liteops-Pipeline__node" design-system/components/organisms/pipeline-viz.html && grep -q "pulse-glow" design-system/components/organisms/pipeline-viz.html && grep -q "liteops-MultiSelect" design-system/components/organisms/multi-select.html && grep -q "liteops-MultiSelect__dropdown" design-system/components/organisms/multi-select.html && grep -q "liteops-Badge" design-system/components/organisms/multi-select.html && grep -q "role=\"combobox\"" design-system/components/organisms/multi-select.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "liteops-Pipeline" pipeline-viz.html finds the component block
    - grep "liteops-Pipeline__node\|liteops-Pipeline__connector" pipeline-viz.html finds node and connector elements
    - grep "pulse-glow" pipeline-viz.html confirms active node animation
    - grep "Collecte documents\|Extraction OCR\|Enrichissement RAG" pipeline-viz.html confirms NAMIBIA pipeline steps
    - grep "liteops-MultiSelect" multi-select.html finds the component block
    - grep "liteops-MultiSelect__trigger\|liteops-MultiSelect__dropdown\|liteops-MultiSelect__option" confirms all sub-elements
    - grep "liteops-Badge" multi-select.html confirms badge composition for tags
    - grep "role=\"combobox\"" multi-select.html confirms accessibility
    - grep "role=\"listbox\"" multi-select.html confirms dropdown accessibility
    - Both files open in browser showing correct components
  </acceptance_criteria>
  <done>Pipeline visualization shows connected vertical nodes with status colors and pulse-glow animation on active nodes. Multi-select shows trigger with badge tags, search input, dropdown with options, and filtered state. Both work in light and dark themes with full accessibility markup.</done>
</task>

</tasks>

<verification>
- Three HTML files exist in design-system/components/organisms/
- Pipeline uses status colors (accent for active, paper-500 for pending, error for failed)
- Pipeline active nodes have pulse-glow animation (only continuous animation per CLAUDE.md)
- Agent card composes Badge and Button atoms
- Multi-select composes Badge and Input atoms
- All use domain-specific French content (NAMIBIA agents, change management tags)
</verification>

<success_criteria>
- Agent card shows AI agent monitoring pattern unique to LiteOps
- Pipeline viz shows NAMIBIA data pipeline with clear node status communication
- Multi-select provides complex input pattern with search and tag management
- All three organisms demonstrate LiteOps domain differentiation
</success_criteria>

<output>
After completion, create `.planning/phases/03-organisms/03-04-SUMMARY.md`
</output>
