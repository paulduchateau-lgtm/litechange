---
phase: 03-organisms
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/components/organisms/data-table.html
  - design-system/components/organisms/modal.html
autonomous: true
requirements: [ORGN-02, ORGN-03]

must_haves:
  truths:
    - "Data table renders with sortable headers in IBM Plex Mono uppercase"
    - "Data table numeric columns are right-aligned with tabular-nums"
    - "Data table has zebra rows, row hover, and horizontal overflow wrapper"
    - "Modal renders with overlay backdrop, centered content, close button, and action bar"
    - "Both organisms work in light and dark themes"
  artifacts:
    - path: "design-system/components/organisms/data-table.html"
      provides: "Complete data table organism"
      contains: "liteops-DataTable"
    - path: "design-system/components/organisms/modal.html"
      provides: "Complete modal dialog organism"
      contains: "liteops-Modal"
  key_links:
    - from: "data-table.html"
      to: "badge.html atom"
      via: "composes .liteops-Badge for status cells"
      pattern: "liteops-Badge"
    - from: "modal.html"
      to: "button.html atom"
      via: "composes .liteops-Button for action bar"
      pattern: "liteops-Button"
---

<objective>
Build the data table and modal organisms. These are the two most complex interactive-pattern organisms used across dashboard and detail views.

Purpose: Data table is the core enterprise component for displaying structured data. Modal is the universal overlay for confirmations, detail views, and forms. Both are high-frequency components in the app.
Output: Two standalone HTML showcase pages in design-system/components/organisms/
</objective>

<execution_context>
@/Users/paulduchateau/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulduchateau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-organisms/03-CONTEXT.md
@design-system/tokens/liteops-tokens.css
@design-system/components/atoms/badge.html
@design-system/components/atoms/button.html

<interfaces>
<!-- Badge atom classes (from badge.html) -->
.liteops-Badge — pill shape, IBM Plex Mono 10px uppercase, colored dot before text
.liteops-Badge--success/--warning/--error/--info/--accent — status variants

<!-- Button atom classes (from button.html) -->
.liteops-Button — inline-flex, DM Sans 500, radius-md
.liteops-Button--primary/--secondary/--ghost/--danger — variants
.liteops-Button--sm/--md/--lg — sizes

<!-- Showcase page pattern (established in Phase 2) -->
- Standalone HTML with <link rel="stylesheet" href="../../tokens/liteops-tokens.css">
- Theme toggle button (fixed top-right, ~8 lines JS)
- .page wrapper (max-width 1140px, padding space-10/space-6)
- .page-header, .section, .section-heading, .section-label, .section-divider
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build data table organism</name>
  <files>design-system/components/organisms/data-table.html</files>
  <read_first>
    - design-system/components/atoms/badge.html (Badge classes for status cells)
    - design-system/tokens/liteops-tokens.css (token references)
    - CLAUDE.md (table component pattern: header IBM Plex Mono 11px UPPERCASE, numeric cells font-mono right-aligned tabular-nums, row hover accent-subtle)
  </read_first>
  <action>
Create standalone HTML showcase page for the data table organism at design-system/components/organisms/data-table.html.

**Data Table component CSS (.liteops-DataTable):**
- BEM: .liteops-DataTable, .liteops-DataTable__wrapper, .liteops-DataTable__header, .liteops-DataTable__row, .liteops-DataTable__cell, .liteops-DataTable__sort-icon
- Wrapper: overflow-x: auto (horizontal scroll on small screens), border: 1px solid var(--liteops-border-primary), border-radius: var(--liteops-radius-md)
- Table: width: 100%, border-collapse: collapse

**Table header (.liteops-DataTable__header):**
- Background: var(--liteops-surface-tertiary)
- Font: var(--liteops-font-mono), 11px, uppercase, letter-spacing: 0.1em
- Color: var(--liteops-text-tertiary)
- Padding: var(--liteops-space-3) var(--liteops-space-4)
- Cursor: pointer on sortable columns
- Sort icon: small chevron SVG (8px), rotated for asc/desc. Use .liteops-DataTable__header.is-sorted-asc and .is-sorted-desc classes

**Table rows:**
- Zebra striping: even rows get var(--liteops-surface-tertiary) at 0.3 opacity (use nth-child(even))
- Row hover: background var(--liteops-accent-subtle)
- Border-bottom: 1px solid var(--liteops-border-secondary)
- Cell padding: var(--liteops-space-3) var(--liteops-space-4)

**Cell types:**
- Text cells: DM Sans 14px, var(--liteops-text-primary)
- Numeric cells (.liteops-DataTable__cell--numeric): font-family var(--liteops-font-mono), text-align: right, font-variant-numeric: tabular-nums
- Status cells: compose .liteops-Badge molecule (copy badge CSS into style block)
- Action cells: compose .liteops-Button--ghost--sm for row actions (3-dot icon or "Voir" text)

**Sample data (French, change management domain):**
Table: "Initiatives en cours" with columns:
| Initiative | Responsable | Statut | Avancement | Signaux | Date |
Use 8-10 rows with realistic French data: "Migration SI Finance", "Refonte process RH", "Transformation digitale agences", etc.
Statut column uses Badge variants (En cours = accent, Termine = success, En attente = warning, Bloque = error).
Avancement column is numeric (percentage).
Signaux column is numeric.

**Showcase sections:**
1. "Default Table" — full table in light theme with sample data, one column sorted
2. "Dark Theme" — same table in data-theme="dark" panel
3. "Overflow Behavior" — table inside a 500px-wide container showing horizontal scroll
4. "Empty State" — table with headers but no rows, showing an empty state message ("Aucune initiative trouvee" centered, tertiary text, with a ghost button "Creer une initiative")
5. "Row Selection" — table with checkbox column and .is-selected row state (accent-subtle background)

**Accessibility:** scope="col" on th elements, overflow wrapper has tabindex="0" and role="region" with aria-label, sort state communicated via aria-sort attribute.
  </action>
  <verify>
    <automated>test -f design-system/components/organisms/data-table.html && grep -q "liteops-DataTable" design-system/components/organisms/data-table.html && grep -q "liteops-Badge" design-system/components/organisms/data-table.html && grep -q "tabular-nums" design-system/components/organisms/data-table.html && grep -q "scope=\"col\"" design-system/components/organisms/data-table.html && grep -q "IBM Plex Mono\|font-mono\|liteops-font-mono" design-system/components/organisms/data-table.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "liteops-DataTable" data-table.html finds the component block class
    - grep "liteops-DataTable__header" data-table.html finds header element
    - grep "liteops-Badge" data-table.html confirms badge composition for status cells
    - grep "tabular-nums" data-table.html confirms numeric formatting
    - grep "scope=\"col\"" data-table.html confirms accessibility on th elements
    - grep "overflow" data-table.html confirms horizontal scroll wrapper
    - grep "nth-child\|zebra\|even" data-table.html confirms zebra striping
    - File opens in browser showing enterprise data table with French data
  </acceptance_criteria>
  <done>Data table renders with IBM Plex Mono uppercase sortable headers, right-aligned numeric columns with tabular-nums, zebra rows, row hover, badge status cells, overflow wrapper, empty state, and row selection. Works in both themes.</done>
</task>

<task type="auto">
  <name>Task 2: Build modal dialog organism</name>
  <files>design-system/components/organisms/modal.html</files>
  <read_first>
    - design-system/components/atoms/button.html (Button classes for action bar)
    - design-system/tokens/liteops-tokens.css (overlay and surface tokens)
  </read_first>
  <action>
Create standalone HTML showcase page for the modal organism at design-system/components/organisms/modal.html.

**Modal component CSS (.liteops-Modal):**
- BEM: .liteops-Modal, .liteops-Modal__overlay, .liteops-Modal__content, .liteops-Modal__header, .liteops-Modal__body, .liteops-Modal__footer, .liteops-Modal__close
- Overlay: position: fixed, inset: 0, background: var(--liteops-surface-overlay), z-index: 50, display: flex, align-items: center, justify-content: center
- Content panel: background: var(--liteops-surface-secondary), border-radius: var(--liteops-radius-lg) (16px), box-shadow: var(--liteops-shadow-2xl), max-height: 85vh, overflow-y: auto, border: 1px solid var(--liteops-border-secondary)

**Size variants:**
- .liteops-Modal--sm: max-width: 400px
- .liteops-Modal--md: max-width: 560px
- .liteops-Modal--lg: max-width: 720px

**Modal header (.liteops-Modal__header):**
- Display: flex, justify-content: space-between, align-items: center
- Padding: var(--liteops-space-6)
- Border-bottom: 1px solid var(--liteops-border-secondary)
- Title: DM Sans 18px, font-weight: 600, var(--liteops-text-primary)
- Close button (.liteops-Modal__close): 32x32px ghost button, X icon (16px SVG), hover: var(--liteops-interactive-ghost-hover), border-radius: var(--liteops-radius-sm)

**Modal body (.liteops-Modal__body):**
- Padding: var(--liteops-space-6)
- Font: DM Sans 14px, var(--liteops-text-secondary), line-height: 1.6

**Modal footer (.liteops-Modal__footer):**
- Display: flex, justify-content: flex-end, gap: var(--liteops-space-3)
- Padding: var(--liteops-space-4) var(--liteops-space-6)
- Border-top: 1px solid var(--liteops-border-secondary)
- Background: var(--liteops-surface-tertiary)
- Compose .liteops-Button--secondary (Annuler) and .liteops-Button--primary (Confirmer) or .liteops-Button--danger (Supprimer)

**Showcase approach:** Since modals are overlays, show them as STATIC positioned elements (not fixed/absolute) within showcase sections, so all variants are visible at once without clicking. Set position: relative on the showcase variants and remove the fixed overlay behavior. Add a note explaining the real implementation uses fixed positioning.

**Showcase sections:**
1. "Size Variants" — show sm, md, lg modals side by side (or stacked) with sample content
2. "Confirmation Dialog" — sm modal with "Supprimer cette initiative ?" title, warning text body, Annuler (secondary) + Supprimer (danger) buttons
3. "Form Modal" — md modal with "Nouvelle initiative" title, form fields in body (compose form-group molecule pattern: label + input + helper text), Annuler + Creer buttons
4. "Detail Modal" — lg modal with "Details de l'initiative" title, longer content with sections
5. "Dark Theme" — md modal in data-theme="dark" panel

**Accessibility:** role="dialog" and aria-modal="true" on content, aria-labelledby pointing to title id, close button has aria-label="Fermer". Focus-visible on all interactive elements.
  </action>
  <verify>
    <automated>test -f design-system/components/organisms/modal.html && grep -q "liteops-Modal" design-system/components/organisms/modal.html && grep -q "liteops-Modal__overlay" design-system/components/organisms/modal.html && grep -q "liteops-Button" design-system/components/organisms/modal.html && grep -q "role=\"dialog\"" design-system/components/organisms/modal.html && grep -q "aria-modal" design-system/components/organisms/modal.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "liteops-Modal" modal.html finds the component block class
    - grep "liteops-Modal--sm\|liteops-Modal--md\|liteops-Modal--lg" modal.html finds all 3 size variants
    - grep "liteops-Modal__overlay" modal.html finds overlay element
    - grep "liteops-Modal__header\|liteops-Modal__body\|liteops-Modal__footer" modal.html finds all 3 sections
    - grep "liteops-Button--primary\|liteops-Button--secondary\|liteops-Button--danger" modal.html confirms button composition
    - grep "role=\"dialog\"" modal.html confirms accessibility role
    - grep "aria-modal=\"true\"" modal.html confirms modal aria attribute
    - File opens in browser showing all modal variants statically rendered
  </acceptance_criteria>
  <done>Modal organism renders with overlay backdrop, 3 size variants (sm/md/lg), close button, header/body/footer sections, composed Button atoms in action bar. Confirmation, form, and detail modal examples shown. Works in both themes.</done>
</task>

</tasks>

<verification>
- Both HTML files exist in design-system/components/organisms/
- Data table uses IBM Plex Mono uppercase headers, tabular-nums for numbers, zebra rows
- Modal has overlay, 3 sizes, close button, action bar with composed buttons
- Both compose Phase 2 atoms (Badge, Button) without hardcoded colors
- scope="col" on data table, role="dialog" + aria-modal on modal
</verification>

<success_criteria>
- Data table looks like a serious enterprise component (Bloomberg-inspired per CONTEXT.md)
- Modal shows all 3 size variants with confirmation, form, and detail examples
- Both work in light and dark themes via theme toggle
</success_criteria>

<output>
After completion, create `.planning/phases/03-organisms/03-02-SUMMARY.md`
</output>
