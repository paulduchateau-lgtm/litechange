---
phase: 03-organisms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/components/organisms/sidebar.html
  - design-system/components/organisms/topbar.html
autonomous: true
requirements: [ORGN-01, ORGN-04]

must_haves:
  truths:
    - "Sidebar renders with olive-900 dark background and lifecycle-phase nav groups"
    - "Sidebar shows IBM Plex Mono 9px uppercase group labels for Cadrer, Observer, Agir, Acteurs, Rendre compte"
    - "Sidebar documents role-based filtering with visual annotations"
    - "Topbar renders with logo, breadcrumb area, user menu, and theme toggle"
    - "Both organisms compose existing atoms/molecules without hardcoded colors"
  artifacts:
    - path: "design-system/components/organisms/sidebar.html"
      provides: "Complete sidebar navigation organism"
      contains: "liteops-Sidebar"
    - path: "design-system/components/organisms/topbar.html"
      provides: "Complete topbar navigation organism"
      contains: "liteops-Topbar"
  key_links:
    - from: "sidebar.html"
      to: "nav-item.html molecule"
      via: "composes .liteops-NavItem and .liteops-NavGroup__label classes"
      pattern: "liteops-NavItem"
    - from: "topbar.html"
      to: "breadcrumb.html molecule"
      via: "composes .liteops-Breadcrumb classes"
      pattern: "liteops-Breadcrumb"
---

<objective>
Build the two app-chrome organisms: sidebar navigation and topbar. These define the application shell layout that Phase 4 pages will consume.

Purpose: Sidebar and topbar are the persistent chrome framing every dashboard page. They must prove that Phase 2 NavItem and Breadcrumb molecules compose correctly into full navigation regions.
Output: Two standalone HTML showcase pages in design-system/components/organisms/
</objective>

<execution_context>
@/Users/paulduchateau/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulduchateau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-organisms/03-CONTEXT.md
@design-system/tokens/liteops-tokens.css
@design-system/components/molecules/nav-item.html
@design-system/components/molecules/breadcrumb.html

<interfaces>
<!-- Nav Item molecule classes (from nav-item.html) -->
.liteops-NavItem — flex row, icon + label, 13px DM Sans, transparent bg
.liteops-NavItem:hover — rgba(255,255,255,0.04) bg, paper-200 text
.liteops-NavItem.is-active — accent-subtle bg, accent-text color
.liteops-NavItem__icon — 15x15px SVG slot
.liteops-NavItem__label — text with ellipsis overflow
.liteops-NavGroup__label — IBM Plex Mono 9px uppercase, olive-500 color, 0.12em spacing

<!-- Breadcrumb molecule classes (from breadcrumb.html) -->
.liteops-Breadcrumb — flex row with gap
.liteops-Breadcrumb__item — DM Sans 13px link
.liteops-Breadcrumb__separator — chevron separator
.liteops-Breadcrumb__current — current page (not a link)

<!-- Button atom classes (from button.html) -->
.liteops-Button — inline-flex, DM Sans 500, radius-md
.liteops-Button--primary/--secondary/--ghost/--danger — variants
.liteops-Button--sm/--md/--lg — sizes

<!-- Showcase page pattern (established in Phase 2) -->
- Standalone HTML with <link rel="stylesheet" href="../../tokens/liteops-tokens.css">
- Theme toggle button (fixed top-right, ~8 lines JS)
- .page wrapper (max-width 1140px, padding space-10/space-6)
- .page-header with .page-title (Source Serif 4, 36px) and .page-subtitle
- .section with .section-heading, .section-label (mono 9px uppercase), .section-divider
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build sidebar navigation organism</name>
  <files>design-system/components/organisms/sidebar.html</files>
  <read_first>
    - design-system/components/molecules/nav-item.html (NavItem + NavGroup classes to reuse)
    - design-system/tokens/liteops-tokens.css (token references)
    - CLAUDE.md (sidebar nav structure: Cadrer, Observer, Agir, Acteurs, Rendre compte groups + role-based filtering rules)
  </read_first>
  <action>
Create standalone HTML showcase page for the sidebar organism at design-system/components/organisms/sidebar.html.

Follow the established Phase 2 showcase page pattern (see button.html for structure): DOCTYPE, Google Fonts link, tokens CSS link (path: ../../tokens/liteops-tokens.css), reset CSS, page layout CSS, theme toggle.

**Sidebar component CSS (.liteops-Sidebar):**
- BEM naming: .liteops-Sidebar, .liteops-Sidebar__header, .liteops-Sidebar__nav, .liteops-Sidebar__footer
- Width: 240px, height: 100vh (or min-height for showcase), position: fixed in real usage
- Background: var(--liteops-olive-900) — sidebar is ALWAYS dark regardless of page theme
- Use data-theme="dark" on the sidebar wrapper element to scope dark tokens
- Padding: var(--liteops-space-6) on sides, var(--liteops-space-8) top
- Border-right: 1px solid rgba(232, 230, 225, 0.07)

**Sidebar header:**
- Logo: "lite" in Source Serif 4 italic 300 weight, hardcoded #C8FF3C; "Ops" in Source Serif 4 regular 400, hardcoded #E8E6E1 (per CLAUDE.md: sidebar logo uses hardcoded colors, never theme tokens)
- Logo font-size: 20px, margin-bottom: var(--liteops-space-8)

**Navigation groups (compose NavItem molecule):**
Reuse .liteops-NavGroup__label and .liteops-NavItem classes from nav-item.html (copy the CSS into this page's style block since showcase pages are standalone).

Full navigation structure from CLAUDE.md:
1. Home section (no group label): "Accueil" nav item
2. "CADRER" group: Initiatives, Diagnostic
3. "OBSERVER" group: Tableau de bord (set as .is-active), Enquetes, Signaux, Connecteurs
4. "AGIR" group: Plan d'action, Suivi projet
5. "ACTEURS" group: Equipes
6. "RENDRE COMPTE" group: Vue COMEX

Each nav item gets a 15x15 inline SVG icon (simple line-art style matching nav-item.html patterns).

**Sidebar footer:**
- User info area at bottom: avatar circle (32px, initials "PD", olive-700 bg), name "Paul Duchateau", role "Consultant Change" in mono 10px tertiary color
- Separated from nav by margin-top: auto (flex column layout)

**Showcase sections:**
1. "Full Sidebar" — complete sidebar at actual 240px width with all groups
2. "Role-Based Filtering" — show 5 sidebar variants side-by-side (narrower, 200px each) labeled by role: COMEX, Sponsor, Consultant Change, Chef de Projet, Collaborateur. Each shows only the nav items that role can see per CLAUDE.md rules. Use a flex row with gap-24 to lay them out. Add a role label above each (IBM Plex Mono 9px uppercase). Gray out or hide items not visible to that role.
3. "Collapsed State" — show sidebar at 60px width with only icons visible (no labels), demonstrating a collapsed variant

**Accessibility:** All nav items are <a> elements with href="#". Focus-visible outline on all items. aria-current="page" on the active item.
  </action>
  <verify>
    <automated>test -f design-system/components/organisms/sidebar.html && grep -q "liteops-Sidebar" design-system/components/organisms/sidebar.html && grep -q "liteops-NavItem" design-system/components/organisms/sidebar.html && grep -q "liteops-NavGroup__label" design-system/components/organisms/sidebar.html && grep -q "CADRER\|Cadrer" design-system/components/organisms/sidebar.html && grep -q "OBSERVER\|Observer" design-system/components/organisms/sidebar.html && grep -q "RENDRE COMPTE\|Rendre compte" design-system/components/organisms/sidebar.html && grep -q "data-theme" design-system/components/organisms/sidebar.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "liteops-Sidebar" sidebar.html finds the component block class
    - grep "liteops-NavItem" sidebar.html finds composed molecule usage
    - grep "liteops-NavGroup__label" sidebar.html finds all 5 lifecycle group labels
    - grep "COMEX\|Sponsor\|Consultant\|Chef de Projet\|Collaborateur" sidebar.html finds role-based filtering section
    - grep "data-theme=\"dark\"" sidebar.html confirms dark scoping on sidebar element
    - grep "#C8FF3C" sidebar.html confirms hardcoded logo color per CLAUDE.md
    - File opens in browser showing 240px dark sidebar with all navigation groups
  </acceptance_criteria>
  <done>Sidebar organism renders at 240px width with olive-900 background, all 5 lifecycle groups with IBM Plex Mono labels, role-based filtering documented visually for all 5 roles, and composes NavItem molecules</done>
</task>

<task type="auto">
  <name>Task 2: Build topbar navigation organism</name>
  <files>design-system/components/organisms/topbar.html</files>
  <read_first>
    - design-system/components/molecules/breadcrumb.html (Breadcrumb classes to reuse)
    - design-system/components/atoms/button.html (Button classes for theme toggle and actions)
    - design-system/tokens/liteops-tokens.css (token references)
  </read_first>
  <action>
Create standalone HTML showcase page for the topbar organism at design-system/components/organisms/topbar.html.

Follow the established Phase 2 showcase page pattern.

**Topbar component CSS (.liteops-Topbar):**
- BEM naming: .liteops-Topbar, .liteops-Topbar__left, .liteops-Topbar__center, .liteops-Topbar__right
- Display: flex, align-items: center, justify-content: space-between
- Height: 56px, padding: 0 var(--liteops-space-6)
- Background: var(--liteops-surface-secondary)
- Border-bottom: 1px solid var(--liteops-border-secondary)
- Position: sticky, top: 0, z-index: 10

**Topbar left section:**
- Logo (content variant): "lite" in Source Serif 4 italic 300, using var(--liteops-accent-text); "Ops" in Source Serif 4 regular 400, using var(--liteops-text-primary) — this is the content variant that uses theme tokens (NOT hardcoded like sidebar)
- Logo font-size: 18px

**Topbar center section:**
- Compose .liteops-Breadcrumb from breadcrumb.html molecule (copy CSS into style block)
- Example: Accueil > Initiatives > Diagnostic ERM

**Topbar right section:**
- Theme toggle: a small button or icon-only button using .liteops-Button--ghost--sm pattern, with sun/moon SVG icon (16px)
- Notification bell: icon-only ghost button with optional red dot indicator (8px circle, absolute positioned)
- User menu: avatar circle (32px) with initials "PD" + dropdown caret icon
- Items separated by gap: var(--liteops-space-3)

**Showcase sections:**
1. "Default Topbar" — full-width topbar in light theme showing logo + breadcrumb + right actions
2. "Dark Theme" — same topbar wrapped in data-theme="dark" panel
3. "With App Shell Context" — topbar positioned above a content area placeholder, with a narrow 240px sidebar stub on the left (just the logo area, demonstrating the 240px offset). Show how topbar sits at left: 240px with width: calc(100% - 240px) when sidebar is present.
4. "Responsive" — topbar at narrower width (600px) showing breadcrumb truncation and compressed right section

**Accessibility:** All interactive elements have focus-visible styles. Bell button has aria-label="Notifications". User menu has aria-label="Menu utilisateur".
  </action>
  <verify>
    <automated>test -f design-system/components/organisms/topbar.html && grep -q "liteops-Topbar" design-system/components/organisms/topbar.html && grep -q "liteops-Breadcrumb" design-system/components/organisms/topbar.html && grep -q "liteops-Button" design-system/components/organisms/topbar.html && grep -q "data-theme" design-system/components/organisms/topbar.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "liteops-Topbar" topbar.html finds the component block class
    - grep "liteops-Topbar__left\|liteops-Topbar__center\|liteops-Topbar__right" topbar.html finds BEM elements
    - grep "liteops-Breadcrumb" topbar.html confirms breadcrumb molecule composition
    - grep "liteops-Button" topbar.html confirms button atom composition
    - grep "accent-text" topbar.html confirms content-variant logo uses theme tokens
    - Both light and dark previews present
    - File opens in browser showing 56px sticky topbar with logo, breadcrumb, and user actions
  </acceptance_criteria>
  <done>Topbar organism renders with logo (content variant using theme tokens), breadcrumb area, theme toggle, notification bell, and user menu. Works in both light and dark themes. App shell context demo shows sidebar+topbar composition.</done>
</task>

</tasks>

<verification>
- Both HTML files exist in design-system/components/organisms/
- Sidebar uses data-theme="dark" scoping and hardcoded logo colors
- Topbar uses theme tokens for content-variant logo
- Both compose Phase 2 molecules (NavItem, Breadcrumb) and atoms (Button)
- No hardcoded colors in component CSS (except sidebar logo per CLAUDE.md exception)
- All text follows typography rules: DM Sans for UI, IBM Plex Mono for labels, Source Serif 4 for headings
</verification>

<success_criteria>
- Sidebar shows complete CLAUDE.md navigation structure with all 5 lifecycle groups
- Role-based filtering is documented visually for all 5 roles
- Topbar shows logo + breadcrumb + user actions at 56px height
- Both organisms open correctly in a browser with theme toggle working
</success_criteria>

<output>
After completion, create `.planning/phases/03-organisms/03-01-SUMMARY.md`
</output>
